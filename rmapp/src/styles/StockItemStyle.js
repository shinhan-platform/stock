import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    backgroundColor: '#333',  // 어두운 배경
    padding: 10,
    marginVertical: 5,
    borderRadius: 5,
  },
  text: {
    color: '#fff',  // 밝은 텍스트 색상
    fontSize: 16,
  },
});
